// W1 plumbing-only placeholder. Real product UI lands in W2 (E4-T*).
// Do NOT add branding, design system imports, or product chrome here.
import type { ReactElement } from 'react';

export function App(): ReactElement {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">LifeOps W1 plumbing test</h1>
    </main>
  );
}
